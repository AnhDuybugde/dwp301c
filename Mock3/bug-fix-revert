Replace YOUR_USERNAME and PR_NUMBER, then run this after creating the pull request:

curl -s https://api.github.com/repos/ibm-developer-skills-network/mcino-Introduction-to-Git-and-GitHub/pulls/PR_NUMBER | grep -E '"html_url"|"state"|"title"|"user"|"head"|"repo"|"full_name"'

Expected output should verify the pull request and show that the head repository is your fork.
